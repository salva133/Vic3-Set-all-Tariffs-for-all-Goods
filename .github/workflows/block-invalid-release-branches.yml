name: Block Invalid Release Branches

on:
  pull_request:
    branches:
      - master

jobs:
  block-invalid-release-pr:
    uses: salva133/My-Workflows/.github/workflows/block-invalid-release-branches.yml@a87abb128d3fb4ebab0268e1e3ed57923346afe3
    with:
      head_ref: ${{ github.head_ref }}
