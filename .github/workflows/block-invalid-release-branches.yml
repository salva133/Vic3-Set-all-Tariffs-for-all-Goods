name: Block Invalid Release Branches

on:
  pull_request:
    branches:
      - master

jobs:
  block-invalid-release-pr:
    uses: salva133/My-Workflows/.github/workflows/block-invalid-release-branches.yml@8499a330dad354724615161a9dfd70ea8c71f80e
    with:
      head_ref: ${{ github.head_ref }}
