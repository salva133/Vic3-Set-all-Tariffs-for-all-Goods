name: Trim Trailing Whitespace

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  trim-whitespace:
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@8499a330dad354724615161a9dfd70ea8c71f80e
