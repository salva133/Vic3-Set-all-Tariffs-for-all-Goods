name: Trim Trailing Whitespace

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  trim-whitespace:
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@a87abb128d3fb4ebab0268e1e3ed57923346afe3
